<UserControl x:Class="Orders.Management.UI.Views.CounterpartyPickView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:local="clr-namespace:Orders.Management.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border BorderThickness="0, 0, 0, 1" BorderBrush="#FFCCCCCC">
            <DockPanel LastChildFill="False" Margin="2">
                <Button DockPanel.Dock="Left"
                        Background="CornflowerBlue"
                        Foreground="LightGray"
                        BorderThickness="0"
                        Width="69"
                        Height="25"
                        Cursor="Hand"
                        Command="{ Binding Path=LoadDataCommand }">
                    Search
                </Button>

            </DockPanel>
        </Border>

        <TextBox Grid.Row="1" x:Name="FilterTextBox" Text="{ Binding Path=SearchString, UpdateSourceTrigger=PropertyChanged }">
        </TextBox>

        <Label Grid.Row="1"
               Padding="11, 8"
               Margin="2"
               Foreground="LightGray"
               VerticalContentAlignment="Center"
               Content="Enter search string..."
               IsHitTestVisible="False">
            <Label.Style>
                <Style TargetType="{ x:Type Label }">
                    <Style.Setters>
                        <Setter Property="Visibility" Value="Collapsed" />
                    </Style.Setters>
                    <Style.Triggers>
                        <DataTrigger Binding="{ Binding Path=Text, ElementName=FilterTextBox }" Value="">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Label.Style>
        </Label>

        <Grid Margin="10" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView x:Name="ItemPicker" SelectionChanged="MyListView_SelectionChanged" Grid.Row="0" 
                  ItemsSource="{ Binding Entities }"
                  SelectedItem="{ Binding Current }"
                  SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="100"/>
                        <GridViewColumn Header="Taxpayer Id" DisplayMemberBinding="{Binding TaxpayerIdentificationNumber}" Width="100"/>

                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="◀ Prev" IsEnabled="{Binding HasPrevPage}" 
                    Command="{Binding PreviousPageCommand}" 
                    Width="75" Margin="5"/>

                <TextBlock VerticalAlignment="Center" Margin="10,0">
                    <Run Text="Page "/>
                    <Run Text="{Binding CurrentPage.Number, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock VerticalAlignment="Center" Margin="10,0">
                    <Run Text="Total "/>
                    <Run Text="{Binding TotalItems, Mode=OneWay}"/>
                </TextBlock>

                <Button Content="Next ▶" IsEnabled="{Binding HasNextPage}" 
                    Command="{Binding NextPageCommand}" 
                    Width="75" Margin="5"/>
            </StackPanel>
            <Button x:Name="ConfirmSelection" Grid.Row="2" Click="Button_Click">Select</Button>
        </Grid>

    </Grid>
</UserControl>
