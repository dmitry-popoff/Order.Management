<UserControl x:Class="Orders.Management.UI.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:local="clr-namespace:Orders.Management.UI.Views"
             xmlns:vm="clr-namespace:Orders.Management.UI.ViewModels"
             xmlns:draw="clr-namespace:System.Drawing;assembly=System.Drawing"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border BorderThickness="0, 0, 0, 1" BorderBrush="#FFCCCCCC">
            <DockPanel LastChildFill="False" Margin="2">
                <Button DockPanel.Dock="Left"
                        Background="CornflowerBlue"
                        Foreground="LightGray"
                        BorderThickness="0"
                        Width="69"
                        Height="25"
                        Cursor="Hand"
                        Command="{ Binding Path=LoadDataCommand }">
                    Search
                </Button>
                <Button DockPanel.Dock="Right"
                        Background="Green"
                        Foreground="LightGray"
                        BorderThickness="0"
                        Width="127"
                        Height="25"
                        Cursor="Hand"
                        Command="{ Binding Path=OpenEditorCommand }">
                    <Button.Content>
                        Create
                    </Button.Content>
                </Button>
            </DockPanel>
        </Border>

        <TextBox Grid.Row="1" x:Name="FilterTextBox" Text="{ Binding Path=SearchString, UpdateSourceTrigger=PropertyChanged }">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="KeyUp">
                    <i:InvokeCommandAction Command="{ Binding FilterCommand }" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </TextBox>

        <Label Grid.Row="1"
               Padding="11, 8"
               Margin="2"
               Foreground="LightGray"
               VerticalContentAlignment="Center"
               Content="Enter search string..."
               IsHitTestVisible="False">
            <Label.Style>
                <Style TargetType="{ x:Type Label }">
                    <Style.Setters>
                        <Setter Property="Visibility" Value="Collapsed" />
                    </Style.Setters>
                    <Style.Triggers>
                        <DataTrigger Binding="{ Binding Path=Text, ElementName=FilterTextBox }" Value="">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Label.Style>
        </Label>

        <Grid Margin="10" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView x:Name="employeesList" 
                        Grid.Row="0"
                  Background="Transparent"
                  AlternationCount="2"
                  SelectionMode="Single"

                  ItemsSource="{ Binding Path=ViewSource }"
                  SelectedItem="{ Binding Path=SelectedItem }"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Path=Id}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Path=CounterpartyTitle}" >
                                <GridViewColumnHeader Click="GridViewColumnHeaderClickedHandler">
                                    Counterparty Title
                                </GridViewColumnHeader>
                            </GridViewColumn>
                            <GridViewColumn Header="Date created" DisplayMemberBinding="{Binding Path=CreatedAt}" />
                            <GridViewColumn Header="Order sum" DisplayMemberBinding="{Binding Path=Sum}" />
                            <GridViewColumn >
                                <GridViewColumnHeader Click="GridViewColumnHeaderClickedHandler">

                                </GridViewColumnHeader>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock HorizontalAlignment="Center" Text="✓" Foreground="Green" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasEmployee}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn >
                            <GridViewColumn Header="Edit" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button BorderThickness="0"                                            
                                            Command="{Binding DataContext.OpenDetailsCommand,
                                                RelativeSource={RelativeSource AncestorType=ListView}}" 
                                             CommandParameter="{Binding}" 

                                            FontSize="10" Background="Transparent"
                                                Foreground="AntiqueWhite"
                                                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                                Width="20" Height="20" Cursor="Hand" >
                                            <Button.Content>
                                                <Image Source="{Binding Source={x:Static draw:SystemIcons.Question}, Converter={StaticResource IconConverter}, Mode=OneWay}" />
                                            </Button.Content>
                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Remove" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>

                                        <Button FontSize="10" BorderThickness="0"
                                            Command="{Binding DataContext.RemoveCommand,
                                                RelativeSource={RelativeSource AncestorType=ListView}}" 
                                             CommandParameter="{Binding}" 
                                                Width="20" Height="20" Cursor="Hand"
                                            Background="Transparent" Foreground="AntiqueWhite"
                                            HorizontalAlignment="Right" Margin="10">
                                            <Button.Content>
                                                <Image Source="{Binding Source={x:Static draw:SystemIcons.Hand}, Converter={StaticResource IconConverter}, Mode=OneWay}" />
                                            </Button.Content>
                                        </Button>

                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                        </GridView.Columns>
                    </GridView>
                </ListView.View>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#F0F4F8"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="◀ Prev" IsEnabled="{Binding HasPrevPage}" 
                    Command="{Binding PreviousPageCommand}" 
                    Width="75" Margin="5"/>

                <TextBlock VerticalAlignment="Center" Margin="10,0">
                    <Run Text="Page "/>
                    <Run Text="{Binding CurrentPage.Number, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock VerticalAlignment="Center" Margin="10,0">
                    <Run Text="Total "/>
                    <Run Text="{Binding TotalItems, Mode=OneWay}"/>
                </TextBlock>

                <Button Content="Next ▶" IsEnabled="{Binding HasNextPage}" 
                    Command="{Binding NextPageCommand}" 
                    Width="75" Margin="5"/>
            </StackPanel>

        </Grid>

    </Grid>
</UserControl>

