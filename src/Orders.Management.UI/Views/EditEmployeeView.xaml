<UserControl x:Class="Orders.Management.UI.Views.EditEmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Orders.Management.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Enter Surname:" />
            <TextBox Text="{Binding Surname}" 
                PreviewTextInput="LettersOnlyTextBox_PreviewTextInput" 
                DataObject.Pasting="LettersOnlyTextBox_Pasting"/>
            <TextBlock Text="Enter Name:" />
            <TextBox Text="{Binding Name}"                 
                PreviewTextInput="LettersOnlyTextBox_PreviewTextInput" 
                DataObject.Pasting="LettersOnlyTextBox_Pasting"/>
            <TextBlock Text="Enter Patronymic:" />
            <TextBox Text="{Binding Patronymic}"
                PreviewTextInput="LettersOnlyTextBox_PreviewTextInput" 
                DataObject.Pasting="LettersOnlyTextBox_Pasting"/>
            <StackPanel Orientation="Horizontal" Margin="10">
                <RadioButton Content="Worker" 
                             IsChecked="{Binding Position, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                    Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Worker}"/>
                <RadioButton Content="Supervisor" 
                             IsChecked="{Binding Position, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                    Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Supervisor}"/>
            </StackPanel>
            <DatePicker 
                SelectedDate="{Binding BirthDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                SelectedDateFormat="Long"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Save" Command="{Binding SaveCommand}" Margin="0,10,0,0" HorizontalAlignment="Left"/>

                <Button Grid.Column="1" Content="Close"  Margin="0,10,0,0" HorizontalAlignment="Right" Height="20" Click="Button_Click" />
            </Grid>
            <Rectangle Height="10" />
            <TextBlock Foreground="{Binding MessageColour}" Text="{Binding Message}"  IsEnabled="{Binding HasError}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
