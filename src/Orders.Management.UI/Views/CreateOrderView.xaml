<UserControl x:Class="Orders.Management.UI.Views.CreateOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:local="clr-namespace:Orders.Management.UI"
             xmlns:view="clr-namespace:Orders.Management.UI.Views"
             xmlns:vm="clr-namespace:Orders.Management.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Enter Sum:" />
            <TextBox Text="{Binding Sum}" 
                PreviewTextInput="DigitsOnlyTextBox_PreviewTextInput" 
                DataObject.Pasting="DigitsOnlyTextBox_Pasting"/>
            <TextBlock Text="Select Employee:" />
            <Button Content="Pick Employee" Command="{Binding PickEmployeeCommand}" Margin="0,10,0,0"/>
            <TextBlock Text="Employee Id: " />
            <TextBlock Text="{Binding EmployeeId}"/>
            <TextBlock Text="{Binding EmployeeName}"/>
            <TextBlock Text="Select Counterparty:" />
            <Button Content="Pick Counterparty" Command="{Binding PickCounterpartyCommand}" Margin="0,10,0,0"/>
            <TextBlock Text="Counterparty Id: " />
            <TextBlock Text="{Binding CounterpartyId}"/>
            <TextBlock Text="{Binding CounterpartyTitle}"/>
            <TextBlock Text="{Binding TaxpayerId}"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Save" Command="{Binding SaveCommand}" Margin="0,10,0,0" HorizontalAlignment="Left"/>

                <Button Grid.Column="1" Content="Close"  Margin="0,10,0,0" HorizontalAlignment="Right" Height="20" Click="Button_Click" />
            </Grid>
            <Rectangle Height="10" />
            <TextBlock Foreground="{Binding MessageColour}" Text="{Binding Message}"  IsEnabled="{Binding HasError}"/>
        </StackPanel>

    </Grid>
</UserControl>
